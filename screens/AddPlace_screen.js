import { StyleSheet, Text, View } from "react-native";
import React from "react";
import PlaceForm from "../components/Places/PlaceForm";

const AddPlace_screen = () => {
  return <PlaceForm />;
};

export default AddPlace_screen;

const styles = StyleSheet.create({});
